import styled from 'styled-components';
import { mainBlue } from '@styles/variables';

const NoAccount = styled.p`
  padding: 2rem 0;
  text-align: center;
  font-weight: bold;
`;

const SignUpSpan = styled.span`
  color: ${mainBlue};
  margin-left: 0.7rem;
`;

export { NoAccount, SignUpSpan };
